# Tech Debt Tracker - TeachFlow AI

Ghi mọi shortcut hoặc debt được chấp nhận trong MVP. Debt không được dùng để che lấp lỗi role permission, class membership, secret exposure, hoặc business rule chưa rõ.

| ID | Ngày | Feature | Trạng thái | Debt / Shortcut | Lý do chấp nhận | Rủi ro | Điều kiện xử lý |
|---|---|---|---|---|---|---|---|
| TD-001 | 2026-06-28 | Harness | Resolved | Ban đầu chưa có frontend/backend nên `init.sh` chỉ verify harness và cảnh báo scaffold còn thiếu. | P0-001 sẽ tạo app scaffold chính thức. | Agent có thể quên bật checks app-level. | Đã xử lý trong P0-001: `init.sh` chạy frontend typecheck/lint/test/build và backend pytest/compile khi project tồn tại. |
| TD-002 | 2026-06-28 | P0-002 Auth | Resolved | P0-002 dùng demo auth nội bộ backend với in-memory session token thay vì Supabase Auth production. | Đủ cho demo accounts và role guard P0; không cần Supabase project để validate slice auth/routing hiện tại. | V2-010 đã thêm production auth provider foundation; local default vẫn demo để test nhanh. | V2-010 thêm `AUTH_PROVIDER=demo|supabase`, Supabase Auth password/refresh/get-user/logout bridge, profile role/organization, invite foundation và giữ role dependency tests. |
| TD-003 | 2026-06-28 | P0-003 Course/Class | Resolved | P0-003 dùng in-memory backend store cho courses, classes và class_students thay vì Supabase/Postgres persistence. | Đủ validate local vertical slice course/class/membership sau khi P0-002 auth đã có role guard. | Production path đã xử lý trong V2-001; local default vẫn memory để test/demo nhanh. | V2-001 thêm `LearningRepository` + Postgres schema cho `courses`, `classes`, `class_students`, RLS enabled/revoke grants và smoke persistence pass. |
| TD-004 | 2026-06-28 | P0-004 RAG | Accepted | P0-004 dùng deterministic local hash embedding 384 chiều và mới pre-ingest 5/10 PDF local đầu tiên vào Supabase. | Đủ để có RAG thật qua Supabase/pgvector, citations và source picker cho demo mà chưa kéo AI provider/OpenAI sang P0-004. | Ranking semantic chưa mạnh như embedding model thật; knowledge base demo chưa bao phủ toàn bộ `data/books/`. | Ở P0-005/P0-006 khi bật AI provider, thay bằng `AIProvider.embed_text()` hoặc embedding model production, chạy lại ingest/resume phần còn lại bằng `scripts/ingest_books.py --start-index 5`. |
| TD-005 | 2026-06-28 | P0-005 Outline | Resolved | Course outlines lưu in-memory trong backend giống course/class demo store. | Nhanh nhất để có AI outline generation và edit/review cho demo trong khung thời gian deploy gấp. | Production path đã xử lý trong V2-003; local default vẫn memory để test/demo nhanh. | V2-003 thêm `ContentRepository` + Postgres schema cho `course_outlines`/`outline_sessions`, RLS enabled/revoke grants và smoke persistence pass. |
| TD-006 | 2026-06-28 | P0-006 Lesson Blocks | Resolved | Lesson sessions/blocks lưu in-memory trong backend. | Đủ để demo Teacher tạo lesson blocks có citations ngay sau outline generation. | Production path đã xử lý trong V2-003; local default vẫn memory để test/demo nhanh. | V2-003 thêm `lesson_sessions`/`lesson_blocks` Postgres persistence va giữ status/citation tests. |
| TD-008 | 2026-06-28 | P0-008 Admin Moderation | Resolved | Admin publish/request-changes state và `admin_feedback` lưu trong in-memory lesson store. | Nhanh nhất để hoàn thành demo Admin moderation nối trực tiếp với Teacher Lesson Studio. | Production path lesson moderation state đã xử lý trong V2-003; audit persistence xử lý trong V2-004. | V2-003 lưu lesson status/admin feedback trong Postgres; V2-004 lưu audit events trong Postgres, giữ role/status transition tests. |
| TD-009 | 2026-06-28 | P0-009 Student Lessons | Resolved | Student published lesson access đọc từ in-memory membership/lesson store. | Đủ để demo class-based access ngay sau Admin publish trong cùng phiên backend. | Production path đã xử lý bằng V2-001 membership persistence và V2-003 lesson persistence; local default vẫn memory. | Student direct-access tests vẫn pass; khi `LEARNING_REPOSITORY=postgres`, membership/lesson reads dùng repository persistent. |
| TD-010 | 2026-06-28 | P0-010 Presentation/PDF | Resolved | PDF export dùng browser print và chưa lưu export record/audit ở backend. | Đủ cho MVP demo deploy gấp; user có thể Save as PDF từ print dialog mà không cần file generation service. | V4-036 đã thêm backend export records/audit cho Markdown/PPTX/PDF; browser print vẫn là delivery nhẹ, chưa thêm server-side renderer vì không cần trong slice này. | Đã xử lý trong V4-036: `lesson_export_records` memory/Postgres repository, `POST/GET /api/v1/lessons/{lesson_id}/exports`, Teacher/Student UI record trước export/print, permission tests Teacher/Student/Admin. |
| TD-011 | 2026-06-28 | P1-004 Audit Events | Resolved | Audit events P1-004 lưu in-memory theo lesson thay vì bảng `moderation_events`/audit table trong Supabase. | Phù hợp với store in-memory hiện tại của lesson/moderation và đủ để Teacher/Admin validate history trong phiên demo. | Production path đã xử lý trong V2-004; query/filter/retention nâng cao vẫn chưa nằm trong V2-P0 slice này. | V2-004 thêm `AuditRepository`, `audit_events` Postgres schema với RLS enabled/revoke grants, service-layer authorization và Supabase smoke pass. |
| TD-012 | 2026-06-28 | P1-005 Upload Books UI | Resolved | Upload PDF ingest đồng bộ trong request backend, chưa có background queue/progress polling. | P1-005 cần vertical slice demo nhanh, file có size guard 20MB và UI hiển thị kết quả completed/failed rõ. | V2-007 đã chuyển upload PDF sang FastAPI `BackgroundTasks` và response/job status `processing/completed/failed`; residual durable worker/raw storage/retry-cancel được tách rõ ở TD-015. | Đã xử lý trong V2-007 Async document ingestion jobs; TD-015 giữ phần production hardening còn lại. |
| TD-013 | 2026-06-28 | V2-005 AI Job Lifecycle | Partially Resolved | V2-005 ghi lifecycle job cho outline/lesson/block generation nhưng vẫn xử lý đồng bộ trong request. V2-014 da them Job Center polling/retry/cancel UI va AI outline/lesson/block retry re-run qua service hien co, nhung initial generation van chua co durable external worker queue. | Cần production observability/persistence trước, tránh đổi architecture quá rộng trong cùng slice; V2-014 dong user-facing retry/cancel truoc khi mo worker architecture lon. | Request AI initial generation dai van co the cho lau; retry background hien dung FastAPI/background in-process, chua phai durable queue sau restart. | Future enhancement: durable worker queue/background processor ben ngoai process web, progress granular, cancel cooperative trong tung phase provider call va token/cost accounting that. |
| TD-014 | 2026-06-28 | V2-006 AI Rate Guard | Accepted | Rate guard hiện là per-user theo job history, chưa có organization-wide budget và chưa tính token/cost thật từ provider usage. | Production organization/auth chưa xong; per-user guard đã giảm rủi ro spam generate ngay và không cần secret/cost provider mới. | Một organization nhiều user vẫn có thể cộng dồn chi phí; cost estimate chỉ là coarse unit. | Sau V2 production auth/organization, thêm org budget, provider usage/token accounting và admin usage view. |
| TD-015 | 2026-06-28 | V2-007 Async Document Ingestion | Partially Resolved | Upload PDF đã chuyển sang FastAPI `BackgroundTasks`, nhưng task vẫn chạy in-process và raw PDF chưa được lưu durable trong Supabase Storage/blob store. V2-014 them Job Center retry/cancel UI va backend guidance: failed `document_upload` thieu raw durable input khong fake retry, user duoc huong dan upload lai file. | Đủ giảm timeout request và hiển thị processing/failed trong V2 slice mà không mở rộng sang storage/worker trong cùng feature. | Nếu backend restart khi task đang chạy, document/job có thể kẹt `processing`; file failed không retry được nếu user không còn file gốc. | Future enhancement: Supabase Storage/raw PDFs durable, worker queue bền vững, cleanup stale `processing`, retry file upload tu storage thay vi yeu cau user upload lai. |
| TD-016 | 2026-06-28 | V2-009 Production Embeddings | Partially Resolved | Re-index document embeddings ban dau van chay dong bo trong request, nhung V2-014 da them Job Center retry/cancel UI va `embedding_reindex` retry thuc thi lai re-index tren cung generation job, co cancel best-effort khong ghi de completed neu job da cancelled. | Cần có provider abstraction, metadata và đường re-index kiểm chứng trước khi mở rộng worker; local/hash và tài liệu nhỏ chạy nhanh đủ cho slice V2-P0 này. | Re-index document nhiều chunk bằng OpenAI vẫn có thể chậm; retry background hien in-process, chua durable sau restart va chua co progress tung chunk. | Future enhancement: durable worker queue, chunk batching/rate control, progress granular va cancel cooperative trong tung batch embedding. |
| TD-017 | 2026-06-29 | V4-028 Knowledge Access Model | Resolved | V4-028 đã tách `library` dài hạn và `contextual` owner-scoped, nhưng contextual docs chưa có TTL/cleanup/quota và raw upload chưa lưu durable trong Supabase Storage/blob store. | Slice này cần sửa logic product/access model trước để không lộ Admin library và không chặn AI grounding; storage governance là feature rộng hơn. | V4-033 đã thêm storage provider boundary metadata/Supabase, contextual TTL/quota, library quota, provenance JSON, Admin export/delete contextual docs owner-scoped va UI labels. Future enhancement: scheduled cleanup, signed raw-download UI, bucket/policy migration va hard-delete workflow co operator confirmation. | Đã xử lý trong V4-033 Knowledge storage governance. |
| TD-018 | 2026-06-29 | V4-029 Auth Registration/JWT | Accepted | V4-029 đã có invite acceptance/register, disabled-profile guard và Supabase signup boundary, nhưng chưa có password reset email, MFA, email verification UX, revoke-all sessions và refresh-token rotation policy matrix. | Slice này ưu tiên sửa workflow product đang thiếu: invited user phải tự kích hoạt tài khoản và JWT cũ phải bị chặn khi profile disabled. Các capability enterprise auth cần Supabase project policy/email setup và scope rộng hơn. | Người dùng quên mật khẩu chưa tự phục hồi trong app; admin chưa có nút revoke toàn bộ phiên; policy refresh token chưa được mô tả đủ trong OpenAPI/runbook. | Tạo feature auth-hardening tiếp theo sau V4-030/V4-031 nếu cần: password reset, MFA, email verification UX, revoke-all sessions, session inventory và regression tests. |
| TD-019 | 2026-07-01 | V4-052 HTML Design Parity | Resolved | Admin pages mới cho `Bài giảng mẫu`, `Báo cáo`, `Nhật ký`, `Cài đặt` ban đầu thiếu backend API riêng cho organization lesson library, report summary, org-wide activity feed và editable safe settings. | User yêu cầu frontend phải kéo backend theo chức năng thật, không được giữ mock/read-only gap state. | Đã xử lý baseline fullstack: backend có `GET /admin/lesson-library`, `GET /admin/reports`, `GET /admin/activity`, `GET/PATCH /admin/settings`; frontend gọi các endpoint này và không expose secrets. Report export/time-series nâng cao nếu cần sẽ là feature riêng. | Resolved trong V4-052 bằng permission/org-scope tests, OpenAPI contract tests, frontend API tests và `./init.sh` pass. |
| TD-020 | 2026-07-01 | V4-052 HTML Multi-part Admin Users | Partially Resolved | `admin-teacher-student-management-design-part2.html` có bulk selection, đổi mật khẩu, khóa tài khoản hàng loạt và xóa account. V4-052 đã thêm bulk selection UI, bulk lock/unlock/soft-delete khỏi active và Supabase password reset email cho active Supabase users. | Hard-delete account thật không nên làm như thao tác table thường vì cần export/delete workflow, revoke/sign-out policy và audit rõ để tránh mất dữ liệu học tập. | Nếu cần xóa vĩnh viễn mà chỉ soft-disable, operator vẫn phải xử lý dữ liệu/account thủ công theo runbook; nếu hard-delete thiếu revoke/session policy có thể lệch kỳ vọng bảo mật. | Future auth/data-deletion slice: chốt hard-delete vs anonymize, export trước khi xóa, revoke sessions, audit event và Supabase Auth admin delete. Bulk parity P0 đã xử lý bằng soft-disable an toàn. |
| TD-021 | 2026-07-01 | BUG-007 Recruiter account shortcuts | Accepted | Login có thể bundle `TEACHFLOW_PUBLIC_*` real account email/password vào frontend để nhà tuyển dụng truy cập nhanh bằng account Supabase/Postgres thật. | User yêu cầu hiển thị 3 tài khoản thật Admin/Teacher/Student để nhà tuyển dụng vào hệ thống dễ dàng, không dùng demo auth. | Nếu dùng nhầm account production hoặc mật khẩu dài hạn, credential sẽ public trong bundle frontend. | Chỉ dùng dedicated QA/recruiter accounts, rotate định kỳ, không gán quyền vượt demo/pilot; trước production public phải bỏ password public hoặc thay bằng invite/magic-link/SSO flow. |
