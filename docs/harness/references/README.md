# References

Thư mục này dành cho tài liệu tham khảo ngắn, thân thiện với agent, khi dự án cần cố định cách dùng một công cụ.

Quy tắc:

- Chỉ thêm reference khi agent cần đọc lặp lại nhiều lần.
- Ưu tiên link/tóm tắt ngắn thay vì copy tài liệu dài.
- Không dùng reference để ghi business rule; business rule thuộc `docs/version1`.
